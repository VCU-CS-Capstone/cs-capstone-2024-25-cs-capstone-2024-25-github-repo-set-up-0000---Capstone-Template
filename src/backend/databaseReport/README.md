# A general report and analysis of the database

## Note
Running `poetry install` without the necessary system depends will result in an error.
You will need postgresql dev libraries to install `psycopg2` .